<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.rg171.music.mapper.RankMapper">
    <resultMap id="BaseResultMap" type="com.rg171.music.entity.Rank">
        <!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
        <result column="id" property="id" jdbcType="BIGINT"/>
        <result column="song_list_id" property="songListId" jdbcType="BIGINT"/>
        <result column="consumer_id" property="consumerId" jdbcType="BIGINT"/>
        <result column="score" property="score" jdbcType="INTEGER"/>
    </resultMap>

    <select id="selectScoreSum" resultType="java.lang.Integer">
        SELECT COALESCE(sum(score), 0) as score from `rank` where song_list_id = #{songListId}
    </select>

    <select id="selectRankNum" resultType="java.lang.Integer">
        SELECT COALESCE(count(id), 0) as num from `rank` where song_list_id = #{songListId}
    </select>

    <insert id="insert" parameterType="com.rg171.music.entity.Rank">
        insert into `rank` (song_list_id, consumer_id,
          score)
        values (#{#{songListId}, #{consumerId},
          #{score})
    </insert>

    <insert id="insertSelective" parameterType="com.rg171.music.entity.Rank">
        insert into `rank`
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="songListId != null">
                songListId,
            </if>
            <if test="consumerId != null">
                consumerId,
            </if>
            <if test="score != null">
                score,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="songListId != null">
                #{songListId},
            </if>
            <if test="consumerId != null">
                #{consumerId},
            </if>
            <if test="score != null">
                #{score},
            </if>
        </trim>
    </insert>
</mapper>
